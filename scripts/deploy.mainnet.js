const hre = require("hardhat");

async function main() {
  const {
    FUND_ADDRESS,
    ADMIN_ADDRESS,
    DEVELOPER_ADDRESS,
    DEVELOPMENT_WALLET
  } = process.env;

  // Add deployment logic and order here ...
}